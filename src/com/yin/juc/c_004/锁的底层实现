synchronized的底层实现
JDK早期的   重量级 --> OS
后来的概念：
    锁升级的概念//TODO 自己补上

sync(object)
    markword 第一个线程来访问的时候， 记录这个线程ID，此时是没有上锁的 (偏向锁)
              如果来了第二个线程争用， 升级为自旋锁，
              10次以后，就升级为重量级锁--> OS

问： 什么时候用自旋锁，什么时候用重量锁？
    多个线程访问的时候，用重量锁，
    执行时间短，线程数少用自旋锁。