线程的概念：
线程的启动方式：
线程的常用方法：

线程同步
-synchronized（object）
    --不能用String常量 “object” Integer（当值改变的时候会生成一个新的对象） Long
    .锁的是对象，不是代码
    .this, XX.class
    .锁定方法，非锁定方法同时执行
    .锁升级
        -偏向锁： 第一个线程来访问的时候， 记录这个线程ID，此时是没有上锁的 (偏向锁)
        -自旋锁： 如果来了第二个线程争用， 升级为自旋锁
        -重量级锁： 10次以后，就升级为重量级锁--> OS